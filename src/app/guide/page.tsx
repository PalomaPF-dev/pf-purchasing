import { requireSession } from "@/lib/session";
import PageHeader from "@/components/PageHeader";

export const dynamic = "force-dynamic";

const SECTIONS: { title: string; body: (string | { list: string[] })[] }[] = [
  {
    title: "このアプリでできること",
    body: [
      { list: [
        "購入品単価の登録申請（取引先ごと・品目ごと）と改訂履歴・改訂理由の管理",
        "申請 → MGR承認 → 部門長承認 の2段階ワークフロー（承認用紙のPDF出力つき）",
        "見積書（PDF/画像）のAI読み取りによる申請フォームへの自動入力",
        "見積書・仕様書の添付（申請詳細・単価履歴の両方から閲覧）",
        "Excel/CSVによる単価申請の一括取込、品番・取引先・社員マスタと担当窓口の一括登録",
        "承認後の単価を mcframe 取込フォーマット（MC取込CSV）で出力",
        "mcframe からエクスポートした現行単価履歴（単価情報）のデータ移行",
      ] },
    ],
  },
  {
    title: "単価申請の流れ",
    body: [
      "1. 「単価申請」→「新規申請」で、まず①発注先を選びます（単価申請は発注先ごとに作成します）。次に②明細で品目CD・品名を検索すると、その発注先の取引品目が候補に出ます。選ぶと品名・単位・ロット・納入場所・現行単価（旧単価）が自動で入ります。履歴にない新規品目はそのまま品目CDを入力できます。",
      "2. 入力方法は3つのタブから選べます。「手入力」は明細表に直接入力、「見積書から取り込み」は Excel・PDF（スキャン画像も可）・画像をAIが読み取り（複数ファイル可・プレビューで確認してから反映）、「Excel/CSVで一括入力」はテンプレートに転記した明細をまとめて追加できます（一括申請）。",
      "3. 明細は表形式で、1行＝1品番です。行の「内訳」ボタン（スライダーのアイコン）を押すと、単価差の内訳（支給材建値・材料建値・単価改定・設計変更・為替変動・その他）と枝番・納品先・単位・ロット・適用終了日を入力できます。内訳の合計は単価差と一致している必要があります。",
      "4. 「申請を提出」すると承認へ回ります。承認者（管理者）は「承認」画面で処理します。段階ごとのタブで対象を切り替え、チェックボックスで複数選択すれば一括承認・一括差し戻しができます。",
      "5. 部門長承認が完了すると、単価履歴に自動反映されます。単価差の要因（内訳）と備考、申請No・申請者も履歴に記録され、後から改訂の経緯をたどれます。",
    ],
  },
  {
    title: "見積書・資料の添付",
    body: [
      "申請フォームの下部にある「添付資料」で、見積書・仕様書などのファイルを添付できます。保存・提出と同時にアップロードされ、申請詳細からいつでも開けます（1ファイル4MBまで・複数可）。",
      "提出後は記録として残るため削除できません（下書き・差し戻し中は削除できます）。追加の添付は申請詳細の「添付資料」から行えます。",
      "承認されて単価履歴に反映されたあとは、単価履歴（品目の改訂履歴）の「添付資料」欄からも同じファイルを開けます。過去の単価がどの見積書に基づいて決まったかを、後からたどれます。",
    ],
  },
  {
    title: "承認用紙（PDF）",
    body: [
      "申請詳細の「承認用紙（PDF）」から、単価申請書（登録品単価連絡書）を表示できます。1行＝1品番で「新単価（適用日・単価・支給単価・買入単価）」と「旧単価（取消日・単価・買入単価）」を左右に並べ、新旧の対比が一目で分かる様式です。承認済みの場合は右上の承認欄（部門長・MGR・担当）に承認印が入ります。",
      "ブラウザの印刷（Ctrl/Cmd + P）で「PDFに保存」を選ぶとPDFファイルとして出力できます。単価差の内訳（改訂理由別）は下部に一覧で並記されます。",
    ],
  },
  {
    title: "MC取込CSV（mcframe連携）",
    body: [
      "部門長承認が完了すると出力できるようになります。申請詳細の「MC取込CSV出力」ボタン（承認済みのみ表示）から、その申請ぶんだけを出力することもできます。フォーマットはMC取込フォーマット（1行目=項目キー、2行目=日本語ラベル、3行目以降=データ）と同一です。",
      "出力されるのは「適用開始日・適用終了日・単価・改訂前単価」の更新で、それ以外の項目（枝番・品名・単位ＣＤ・ロット数・通貨ＣＤ・納入場所ＣＤ・納品先ＣＤ・ワーキンググループＣＤ・仕入税ＣＤ・メモ）は mcframe の現行登録値をそのまま維持して出力します。申請で明示的に入力した場合のみ、その値で上書きされます。",
      "出力時に「出力済み」を記録すると、次回は未出力の明細だけが表示されます（再出力も可能です）。",
    ],
  },
  {
    title: "初期データ移行（運用開始時の1回のみ）",
    body: [
      "「初期データ移行」は、運用開始時に過去の単価履歴を引き継ぐための一度きりの作業です。mcframe からエクスポートした単価情報（CSV UTF-8保存）を取り込みます。",
      "移行後は、単価の登録・改訂はすべて「単価申請 → MGR承認 → 部門長承認」で行い、承認された単価が単価履歴に積み上がっていきます。日常運用でこの画面を使うことはありません。",
      "単価情報には品名・発注先名が含まれないため、品番マスタ・取引先マスタを取り込んだ後に「マスタから名称を補完する」を実行してください。",
    ],
  },
  {
    title: "承認ワークフローの設定（管理者）",
    body: [
      "「承認W/F設定」で、承認段階（2段階＝MGR承認→部門長承認 / 1段階＝MGR承認のみ）を切り替えられます。1段階にすると、1回の承認で確定し単価履歴に反映されます。",
      "段階の名称（既定は「MGR」「部門長」）も変更でき、画面と申請書（登録品単価連絡書）の承認欄に反映されます。",
      "承認者を社員番号で指定すると、その段階はその人だけが承認・差し戻しできます。空欄にすると、すべての管理者が承認できます（既定）。",
    ],
  },
  {
    title: "社員マスタとユーザー登録（管理者）",
    body: [
      "「社員マスタ」に社員番号・氏名・承認W/F（MGR／部門長）・権限（管理者／一般）を登録します。社員一覧のExcelは、一括取込の「社員マスタの一括登録」からそのまま取り込めます（シート名が「社員一覧」なら自動で選ばれます）。",
      "登録後に「社員マスタからユーザーを登録 / 更新」を押すと、ログインユーザーがまとめて作成され、承認W/F設定の承認者（MGR・部門長）も社員マスタの内容で設定されます。既存ユーザーは氏名と権限だけが更新され、パスワードは変わりません。",
      "社員マスタの氏名は、取引先の担当窓口を取り込むときの名寄せ（氏名→社員番号）にも使います。担当窓口より先に社員マスタを取り込んでください。",
    ],
  },
  {
    title: "権限と担当発注先",
    body: [
      { list: [
        "一般（member＝バイヤー・チェイサー）: 自分が担当する発注先のみ、単価申請の作成・提出と単価履歴の閲覧ができます。担当外の発注先は検索候補にも出ず、申請もできません。",
        "管理者（admin): すべての発注先を扱えます。承認・差し戻し、マスタ管理、一括取込、MC出力、データ移行も管理者のみです。",
      ] },
      "取引先1件につき、企画グループ（バイヤー主担当・副担当）と管理グループ（チェイサー）の3名まで担当を設定でき、この3名がその発注先を扱えます（マスタ登録はチェイサーが入力・バイヤーが承認という運用に合わせています）。",
      "割当は「取引先マスタ」の担当欄をクリックして社員番号を入力します（管理者のみ）。すべて空にすると未割当（管理者のみが扱える状態）に戻ります。",
      "多数の発注先をまとめて割り当てる場合は、一括取込の「取引先の担当窓口」から、「取引先CD／取引先名／企画グループ／管理グループ」の一覧をそのまま取り込めます。氏名は社員マスタで社員番号に自動変換され、「町野 真一（髙橋 彩佳）」のような併記は主担当・副担当に分けて登録されます。",
    ],
  },
];

/** 使い方ガイド */
export default async function GuidePage() {
  await requireSession();
  return (
    <div className="mx-auto max-w-3xl p-4 sm:p-6">
      <PageHeader title="使い方" description="PF購買単価の基本操作ガイド" />
      <div className="space-y-4">
        {SECTIONS.map((s) => (
          <section key={s.title} className="rounded-xl border border-[#e5e5e5] bg-white p-5">
            <h2 className="mb-3 text-sm font-bold text-[#333333] after:mt-1.5 after:block after:h-[3px] after:w-8 after:rounded-full after:bg-[#e11d48] after:content-['']">
              {s.title}
            </h2>
            <div className="space-y-2 text-sm leading-relaxed text-[#555555]">
              {s.body.map((b, i) =>
                typeof b === "string" ? (
                  <p key={i}>{b}</p>
                ) : (
                  <ul key={i} className="list-disc space-y-1 pl-5">
                    {b.list.map((li) => (
                      <li key={li}>{li}</li>
                    ))}
                  </ul>
                )
              )}
            </div>
          </section>
        ))}
      </div>
    </div>
  );
}
